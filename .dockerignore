# DATA/ is intentionally NOT ignored here (the build bakes it into the page); it
# stays gitignored so it never reaches GitHub, and the built image goes only to
# the PRIVATE GHCR package. Keep the rest of the context small.
.venv/
.git/
outputs/
dashboard/dist/
**/__pycache__/
*.pyc
.pytest_cache/
.DS_Store
tests/
native/
windows/
docs/
SIMULATION_HANDOFF.md
